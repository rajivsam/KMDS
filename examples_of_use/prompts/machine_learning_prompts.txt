ML EDA Phase:
Prompt: Review the contents of the notebook and prepare the table of contents for the notebook. 
Note: The above prompt worked fine.
Notes About Response:
When asked to summarize the notes, I got this reponse which is not quite right:
The primary data quality issues stemmed from inconsistent data entry in categorical fields and the presence of significant outliers in several
 key numerical columns. These anomalies required substantial preprocessing and likely introduced some bias into the analysis.
  Furthermore, the handling of missing values through mean imputation may have suppressed some of the natural variance in the dataset.

RS: Chose not to include this because this is incorrect.
ML Modelling Choice:
To understand which products to include, we compute the quarterly revenue and then compute the proportion of the quarterly revenue each product
contributes. This way we can pick a threshold for the contribution and then drop the products that do not contribute. In this analysis we pick the
products that contribute upto 80 percent of the total revenue. To do this we compute the contribution of each product and then sort it. Then we do
a cumulative sum on the sorted values. The products that are within the 80 percent threshold are used for this analysis.

ML Modeling Phase
PCA results show that with 30 components, we can explain most of the variation in Q1 sales at the store. We can profile pricincal components
in the descending order of variance explained to understand the inventory components that dominate each principal component. Can you summarize this.

